[project]
name = "rcd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=5.0.1",
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "marimo>=0.23.16",
]
